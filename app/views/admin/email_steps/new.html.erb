<% content_for :title, t("admin.email_steps.new") %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="mb-6">
    <%= link_to "← #{t('admin.email_steps.back_to_sequence')}", admin_email_sequence_path(@sequence), class: "text-blue-600 hover:underline" %>
  </div>

  <h1 class="text-3xl font-bold mb-6"><%= t("admin.email_steps.new") %></h1>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render "form" %>
  </div>
</div>
