<% content_for :title, t("admin.referral_reward_tiers.edit") %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="mb-6">
    <%= link_to "← #{t('admin.referral_reward_tiers.back_to_list')}", admin_referral_reward_tiers_path, class: "text-blue-600 hover:underline" %>
  </div>

  <h1 class="text-3xl font-bold mb-6"><%= t("admin.referral_reward_tiers.edit") %></h1>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render "form" %>
  </div>
</div>
