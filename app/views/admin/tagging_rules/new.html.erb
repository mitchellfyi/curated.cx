<div class="py-8">
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900"><%= t('admin.tagging_rules.new') %></h1>
      <p class="mt-2 text-gray-600"><%= t('admin.tagging_rules.new_description') %></p>
    </div>

    <div class="bg-white shadow rounded-lg p-6">
      <%= render 'form', tagging_rule: @tagging_rule, taxonomies: @taxonomies %>
    </div>
  </div>
</div>
