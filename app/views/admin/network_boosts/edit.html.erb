<% content_for :title, t("admin.network_boosts.edit") %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <%= link_to "&larr; #{t('admin.network_boosts.back')}".html_safe, admin_network_boosts_path, class: "text-gray-600 hover:underline" %>
  <h1 class="text-3xl font-bold mt-2 mb-6"><%= t("admin.network_boosts.edit") %></h1>

  <%= render "form", boost: @boost, available_sites: @available_sites %>
</div>
