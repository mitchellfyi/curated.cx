<%# Comments list for a content item %>
<div class="max-w-3xl mx-auto px-4 py-8">
  <%# Breadcrumb %>
  <nav class="mb-6">
    <%= link_to feed_index_path, class: "text-sm text-gray-500 hover:text-blue-600 transition-colors flex items-center gap-1" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
      </svg>
      <%= t('comments.back_to_feed') %>
    <% end %>
  </nav>

  <%# Content item summary %>
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
    <h1 class="text-xl font-semibold text-gray-900 mb-2">
      <%= link_to @content_item.title || t('feed.content.untitled'),
                  safe_external_url(@content_item.url_canonical),
                  target: "_blank",
                  rel: "noopener noreferrer",
                  class: "hover:text-blue-600 transition-colors" %>
    </h1>
    <% if @content_item.source.present? %>
      <p class="text-sm text-gray-500"><%= @content_item.source.name %></p>
    <% end %>
  </div>

  <%# Comments section %>
  <div class="bg-white rounded-lg shadow-sm border border-gray-200">
    <div class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-lg font-medium text-gray-900">
        <%= t('comments.comment_count', count: @comments.size) %>
      </h2>
    </div>

    <%# Locked message %>
    <% if @content_item.comments_locked? %>
      <div class="px-6 py-4 bg-amber-50 border-b border-amber-100">
        <p class="text-sm text-amber-800">
          <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
          </svg>
          <%= t('comments.locked_message') %>
        </p>
      </div>
    <% end %>

    <%# Comment form %>
    <% if user_signed_in? %>
      <% if policy(Comment).create? && !@content_item.comments_locked? %>
        <div class="px-6 py-4 border-b border-gray-200">
          <%= render 'comments/form',
                     comment: Comment.new,
                     commentable: @content_item %>
        </div>
      <% end %>
    <% else %>
      <div class="px-6 py-4 border-b border-gray-200 text-center">
        <%= link_to t('comments.sign_in_to_comment'),
                    new_user_session_path,
                    class: "text-blue-600 hover:text-blue-800" %>
      </div>
    <% end %>

    <%# Comments list %>
    <%= turbo_frame_tag "comments_list" do %>
      <div class="divide-y divide-gray-100">
        <% if @comments.any? %>
          <% @comments.each do |comment| %>
            <div class="px-6">
              <%= render 'comments/comment', comment: comment, depth: 0 %>
            </div>
          <% end %>
        <% else %>
          <div class="px-6 py-12 text-center text-gray-500">
            <svg class="mx-auto w-12 h-12 text-gray-300 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            <p><%= t('comments.empty') %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
