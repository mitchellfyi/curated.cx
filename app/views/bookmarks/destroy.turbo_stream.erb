<%= turbo_stream.replace "bookmark_button_#{@bookmarkable.class.name.underscore}_#{@bookmarkable.id}" do %>
  <%= render "bookmarks/button", bookmarkable: @bookmarkable %>
<% end %>

<%# Also remove from My Saves page if present %>
<%= turbo_stream.remove dom_id(@bookmark) %>
