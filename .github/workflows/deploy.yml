name: Deploy to Dokku

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      skip_migrations:
        description: 'Skip database migrations'
        required: false
        default: 'false'
        type: boolean
      force_deploy:
        description: 'Force deploy even if health check fails'
        required: false
        default: 'false'
        type: boolean
      rollback_sha:
        description: 'Deploy a specific commit SHA (for rollback). Leave empty for HEAD.'
        required: false
        default: ''
        type: string

concurrency:
  group: deploy-production
  cancel-in-progress: true

env:
  APP_NAME: curated
  DOKKU_HOST: ${{ secrets.DOKKU_HOST }}
  APP_URL: ${{ secrets.APP_URL || 'https://curated.cx' }}

jobs:
  # Require CI to pass before deployment
  ci:
    name: Run CI
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      actions: read

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [ci]
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0  # Full history for better git push

      - name: Resolve deploy ref
        id: resolve_ref
        run: |
          if [ -n "${{ inputs.rollback_sha }}" ]; then
            echo "deploy_sha=${{ inputs.rollback_sha }}" >> $GITHUB_OUTPUT
            echo "🔄 Rolling back to commit: ${{ inputs.rollback_sha }}"
          else
            echo "deploy_sha=${{ github.sha }}" >> $GITHUB_OUTPUT
            echo "🚀 Deploying HEAD: ${{ github.sha }}"
          fi

      - name: Setup SSH
        uses: webfactory/ssh-agent@a6f90b1f127823b31d4d4a8d96047790581349bd # v0.9.1
        with:
          ssh-private-key: ${{ secrets.DOKKU_SSH_PRIVATE_KEY }}

      - name: Add Dokku to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.DOKKU_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Pre-deployment health check
        id: pre_health
        continue-on-error: true
        run: |
          echo "Checking if app is currently running..."
          HEALTH_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.APP_URL }}/up" --max-time 10 || echo "000")
          echo "health_status=$HEALTH_STATUS" >> $GITHUB_OUTPUT
          if [ "$HEALTH_STATUS" = "200" ]; then
            echo "✅ App is healthy (HTTP $HEALTH_STATUS)"
          else
            echo "⚠️ App returned HTTP $HEALTH_STATUS (may be first deploy)"
          fi

      - name: Backup database before deploy
        if: inputs.skip_migrations != 'true'
        continue-on-error: true
        run: |
          echo "Creating database backup before deployment..."
          BACKUP_NAME="pre-deploy-$(date +%Y%m%d-%H%M%S)"
          ssh -o StrictHostKeyChecking=no dokku@${{ secrets.DOKKU_HOST }} postgres:export ${{ env.APP_NAME }}-db > /tmp/db-backup-$BACKUP_NAME.dump 2>/dev/null || echo "Backup skipped (no existing database or first deploy)"
          echo "Backup created: $BACKUP_NAME"

      - name: Deploy to Dokku
        id: deploy
        run: |
          DEPLOY_SHA="${{ steps.resolve_ref.outputs.deploy_sha }}"
          echo "🚀 Deploying commit $DEPLOY_SHA to Dokku..."
          git remote add dokku dokku@${{ secrets.DOKKU_HOST }}:${{ env.APP_NAME }} 2>/dev/null || git remote set-url dokku dokku@${{ secrets.DOKKU_HOST }}:${{ env.APP_NAME }}

          # Push with force to handle any divergent histories
          if git push dokku ${DEPLOY_SHA}:refs/heads/main --force 2>&1 | tee /tmp/deploy.log; then
            echo "deploy_success=true" >> $GITHUB_OUTPUT
            echo "✅ Code pushed successfully"
          else
            echo "deploy_success=false" >> $GITHUB_OUTPUT
            echo "❌ Deploy failed"
            cat /tmp/deploy.log
            exit 1
          fi
        env:
          GIT_SSH_COMMAND: "ssh -o StrictHostKeyChecking=no"

      - name: Wait for app to start
        run: |
          echo "Waiting for app to start..."
          sleep 10

      - name: Run database migrations
        if: inputs.skip_migrations != 'true'
        id: migrations
        run: |
          echo "Running database migrations..."
          if ssh -o StrictHostKeyChecking=no dokku@${{ secrets.DOKKU_HOST }} run ${{ env.APP_NAME }} rails db:migrate db:seed 2>&1 | tee /tmp/migrate.log; then
            echo "migration_success=true" >> $GITHUB_OUTPUT
            echo "✅ Migrations completed"
          else
            echo "migration_success=false" >> $GITHUB_OUTPUT
            echo "❌ Migration failed"
            cat /tmp/migrate.log
            exit 1
          fi

      - name: Post-deployment health check
        id: post_health
        run: |
          echo "Running post-deployment health check..."
          MAX_RETRIES=6
          RETRY_DELAY=10

          for i in $(seq 1 $MAX_RETRIES); do
            HEALTH_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.APP_URL }}/up" --max-time 15 || echo "000")
            echo "Attempt $i: HTTP $HEALTH_STATUS"

            if [ "$HEALTH_STATUS" = "200" ]; then
              echo "health_check=passed" >> $GITHUB_OUTPUT
              echo "✅ Health check passed!"
              exit 0
            fi

            if [ $i -lt $MAX_RETRIES ]; then
              echo "Waiting ${RETRY_DELAY}s before retry..."
              sleep $RETRY_DELAY
            fi
          done

          echo "health_check=failed" >> $GITHUB_OUTPUT
          echo "❌ Health check failed after $MAX_RETRIES attempts"

          if [ "${{ inputs.force_deploy }}" = "true" ]; then
            echo "⚠️ Force deploy enabled, continuing despite health check failure"
            exit 0
          fi
          exit 1

      - name: Rollback on failure
        if: failure() && steps.pre_health.outputs.health_status == '200'
        run: |
          echo "🔄 Attempting rollback..."
          # Get the previous release
          PREVIOUS_TAG=$(ssh -o StrictHostKeyChecking=no dokku@${{ secrets.DOKKU_HOST }} tags:list ${{ env.APP_NAME }} 2>/dev/null | tail -2 | head -1 || echo "")

          if [ -n "$PREVIOUS_TAG" ]; then
            echo "Rolling back to $PREVIOUS_TAG..."
            ssh -o StrictHostKeyChecking=no dokku@${{ secrets.DOKKU_HOST }} tags:deploy ${{ env.APP_NAME }} $PREVIOUS_TAG || echo "Rollback command failed"
          else
            echo "No previous release found, cannot rollback"
          fi

      - name: Deploy summary
        if: always()
        run: |
          DEPLOY_SHA="${{ steps.resolve_ref.outputs.deploy_sha }}"
          TIMESTAMP=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          STATUS="${{ job.status }}"

          echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Detail | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Status** | ${STATUS} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${DEPLOY_SHA}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | production |" >> $GITHUB_STEP_SUMMARY
          echo "| **App** | ${{ env.APP_NAME }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **URL** | ${{ env.APP_URL }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Timestamp** | ${TIMESTAMP} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered by** | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Rollback SHA** | ${{ inputs.rollback_sha || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY

      - name: Notify on success
        if: success()
        run: |
          echo "✅ Deployment successful!"
          echo "App: ${{ env.APP_NAME }}"
          echo "URL: ${{ env.APP_URL }}"
          echo "Commit: ${{ steps.resolve_ref.outputs.deploy_sha }}"
          echo "Deployed by: ${{ github.actor }}"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "App: ${{ env.APP_NAME }}"
          echo "Commit: ${{ steps.resolve_ref.outputs.deploy_sha }}"
          echo "Check the logs above for details."
