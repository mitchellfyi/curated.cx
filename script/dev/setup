#!/usr/bin/env ruby
# Development setup script

puts "🚀 Setting up development environment..."
puts "=" * 50

# Bundle install
puts "\n📦 Installing gems..."
system("bundle install") or exit(1)

# Database setup
puts "\n🗄️  Setting up database..."
system("rails db:create db:migrate db:seed") or exit(1)

# Install yarn dependencies
puts "\n📦 Installing JavaScript dependencies..."
system("yarn install") or exit(1)

# Precompile assets for development
puts "\n🎨 Precompiling assets..."
system("rails assets:precompile") or exit(1)

puts "\n✅ Development setup complete!"
puts "\n💡 You can now run:"
puts "   rails server    # Start the Rails server"
puts "   ./script/dev/quality  # Run code quality checks"
